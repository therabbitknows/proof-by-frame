# Slim public subset — pins for the helper modules in backend/app/.
# The full backend pulls in additional deps (Discord bot, ORM, image
# pipeline, etc.) that are not part of this public release.

httpx
fastapi

# Solana integration
solders>=0.20.0
base58>=2.1.1
PyNaCl>=1.5.0
