#!/usr/bin/env node
/**
 * setup_merkle_tree.mjs (contract-only public release)
 *
 * Purpose: create a Bubblegum merkle tree to hold compressed NFTs.
 * One-time setup per cluster. Tree config (depth/buffer/canopy) chosen
 * for ~16k-leaf capacity.
 *
 * Reference primitive: https://developers.metaplex.com/bubblegum
 *
 * Implementation private. See README in this dir.
 */

console.error("Contract-only public stub. See backend/scripts/admin/README.md.");
process.exit(2);
